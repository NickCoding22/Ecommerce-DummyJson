

function AddToCart (props) {

    const handleAdd = (() => {
        return 0;
    });

    return (
        <button
            onClick={handleAdd}
            text = "Add to Cart">
        </button>
    )
};

export default AddToCart;